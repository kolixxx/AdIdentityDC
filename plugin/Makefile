# NOT usable from this repository. `../../Mk/plugins.mk` exists only inside the
# opnsense/plugins ports tree, so `make` here fails on the include. Kept as the
# starting point for a real FreeBSD package later (PROJECT_STATE.md, D12).
#
# To install on a firewall now, use ./install.sh from a checkout on the box.

PLUGIN_NAME=		adidentity
PLUGIN_VERSION=		0.1.0
PLUGIN_COMMENT=		AD identity to OPNsense aliases (pilot)
PLUGIN_MAINTAINER=	dev@local
PLUGIN_DEPENDS=

.include "../../Mk/plugins.mk"
